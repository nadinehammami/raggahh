# IA Models
PDF_MODEL=mistral
IMAGE_MODEL=llava

# Prompts
PDF_PROMPT_PREFIX=Tu es un expert en analyse de documents. Je vais te donner un texte (en français ou en autre). Fournis un résumé en français détaillé qui comprend : 1) Le sujet principal, 2) Les points clés, 3) Les conclusions ou recommandations importantes.
IMAGE_PROMPT_PREFIX=Décris cette image de manière détaillée en français. Mentionne les éléments visuels, les couleurs, les actions en cours et le contexte éventuel.

# Lightweight models and expected env var names for backend/server.js
MODEL_PDF=llama3.2:1b
MODEL_IMG=llava:7b
PROMPT_PDF=Tu es un expert en analyse de documents. Je vais te donner un texte (en français ou en autre). Fournis un résumé en français détaillé qui comprend : 1) Le sujet principal, 2) Les points clés, 3) Les conclusions ou recommandations importantes.
PROMPT_IMG=Décris cette image de manière détaillée en français. Mentionne les éléments visuels, les couleurs, les actions en cours et le contexte éventuel.

# PDF Processing Mode
# Ici, dans ton code tu as des flags USE_PDF_PARSE=true/false et USE_OCR=true/false,
# donc adapte en conséquence (exemple ci-dessous)
USE_PDF_PARSE=true
USE_OCR=true

# UI-related modals (à utiliser côté frontend, c’est bien)
ENABLE_IMAGE_MODAL=true
ENABLE_PDF_MODAL=true

# Port serveur backend
PORT=3001

# Adresse du service Ollama (à ajouter si utilisé dans backend)
OLLAMA_HOST=http://ollama_ai:11434



